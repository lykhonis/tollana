[package]
name = "tollana-host"
version = "0.1.0"
edition = "2021"
description = "Tollana host process: plugin identity map, scheduler, and equal packages"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tollana-core = { path = "../tollana-core" }
